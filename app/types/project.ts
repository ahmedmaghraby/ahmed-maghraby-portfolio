export type ProjectProps = {
  id: number;
  name: string;
  description: string;
  github: string;
  demo: string;
  image: string;
  available: boolean;
};
// Create an array of objects with key and icon
